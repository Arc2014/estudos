'use strict';

/**
 * @ngdoc service
 * @name genesisApp.primeiroService
 * @description
 * # primeiroService
 * Service in the genesisApp.
 */
angular.module('genesisApp')
  .service('primeiroService', function () {
    // AngularJS will instantiate a singleton by calling "new" on this function
  });
